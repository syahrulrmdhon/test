const initialState = {
    basic: {},
    component: null,
};

export default function reducer(state = initialState, action = {}) {
    switch(action.type){
        case 'SET_SUBJECT':

        default:
            return state 
    }
}